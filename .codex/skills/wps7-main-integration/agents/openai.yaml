interface:
  display_name: "WPS7 Main Integration"
  short_description: "Commit, merge, align and deploy WPS7 worktrees"
  default_prompt: "Use $wps7-main-integration to commit completed work, merge it into main, align all worktrees, and safely deploy the new WPS7 executable."
